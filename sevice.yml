apiVersion: v1
kind: Service
metadata:
  creationTimestamp: null
  labels:
    run: nginx-test
  name: nginx-test-clusterip
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 80
  selector:
    run: nginx-test
  type: ClusterIP
status:
  loadBalancer: {}
